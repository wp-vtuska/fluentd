<match **>
 type sumologic
 endpoint "#{ENV['SUMO_COLLECTOR_URL']}"
 log_format "#{ENV['SUMO_LOG_FORMAT']}"
 flush_interval "#{ENV['SUMO_FLUSH_INTERVAL']}"
 num_threads "#{ENV['SUMO_NUM_THREADS']}"
</match>
